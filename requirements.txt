python-telegram-bot[rate-limiter]
yfinance
pandas
numpy
python-dotenv
smartapi-python
# smartapi-python transitive deps — pinned because they don't always install cleanly
logzero
websocket-client
six
pyotp
requests
pytz
# Tip parser: text-only works out of the box. For photo OCR, the Tesseract
# binary must also be installed on the host (Windows: UB-Mannheim build;
# Linux: `sudo apt install tesseract-ocr`).
pytesseract
Pillow
# Telethon: listen to public Telegram channels via your USER account
# (separate from Bot API). Used by telethon_listener.py for channel monitoring.
telethon

# --- Dev / CI only (not needed at runtime) ---
# Test runner for tests/. CI installs a minimal subset (pandas numpy pytz
# python-dotenv pytest) rather than the full file — see .github/workflows/ci.yml.
pytest
