# Secrets — never commit
.env
.env.*
!env.example.txt

# Telethon USER-ACCOUNT session — this IS a credential (account access). Never commit.
telethon_session.session
*.session
*.session-journal

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv/

# Cache files
angel_scrip_master.json
*.log
logs/

# Runtime SQLite (subscribers, fired-signal dedup) — per-deployment state
stockagent.db
*.db-journal
*.db-wal
*.db-shm

# Point-in-time universe snapshots (per-deployment runtime data)
universe_snapshots/

# IDE / OS
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db

# Distribution / packaging
build/
dist/
*.egg-info/
